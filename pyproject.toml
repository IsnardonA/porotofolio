[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio-data-pipeline"
version = "0.1.0"
description = "Local data pipeline project for Docker-only usage"
requires-python = ">=3.11"
dependencies = []
scripts = { make = "portfolio_make:main" }
